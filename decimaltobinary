#include <iostream>
#include <math.h>

using namespace std;

void decimal_to_binary(int &n)
{
    long long nhiphan=0;
    int p=0;
    while (n>0)
    {
        nhiphan += (n%2)*pow(10, p);
        ++p;
        n/=2;
    }
    cout << nhiphan;
}
